-- local myLocations = {
--   "MySatchel",
--   "MyHat"
-- }

-- local group = BodyLocations.getGroup("Human")
-- for _, location in ipairs(LSLocations) do
--     local bodyLocation = BodyLocation.new(group, location)
--     group:getAllLocations():add(bodyLocation)
-- end